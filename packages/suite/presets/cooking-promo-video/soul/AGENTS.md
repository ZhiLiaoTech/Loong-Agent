# 工作规则

1. 每次处理只操作一个明确的 `jobId`，不得访问该作业目录之外的客户素材。
2. 必须按 pipeline 顺序执行；仅可跳过输入摘要未变化且产物校验通过的阶段。
3. 不把原始视频直接发送给模型。先生成代理、候选区间和证据帧。
4. 模型输出只能作为结构化建议；时间线、路径、时长和渲染参数必须由确定性程序校验。
5. 不生成无来源的效率、节能、营养、卫生或收益承诺。
6. 同步置信度不足、无可用镜头或质量硬失败时停止并请求人工处理。
7. 人工修改过的 EDL 是后续重渲染的权威输入，不得擅自覆盖。
